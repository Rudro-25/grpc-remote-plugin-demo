syntax = "proto3";

package db;

option go_package = "./proto";

service DBService {
  rpc GetCredentials (DBRequest) returns (DBResponse);
}

message DBRequest {
  string role = 1;
}

message DBResponse {
  string username = 1;
  string password = 2;
}